{
  "name": "@rtrentjones/greenlight-keepalive",
  "version": "0.2.25",
  "private": true,
  "description": "Greenlight keepalive — a Cloudflare Worker cron that pings data:supabase projects so they never hit the 7-day idle pause, and alerts on failure.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/RTrentJones/greenlight.git",
    "directory": "packages/keepalive"
  },
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "build": "tsup && cp dist/index.js ../../infra/modules/keepalive/worker.js",
    "dev": "wrangler dev"
  }
}
