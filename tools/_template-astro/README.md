# `_template-astro` (placeholder)

Lane template for **Astro on Cloudflare Workers** — verify mode `api` (+ light `playwright`).

> **Phase 0:** placeholder only. Real template content is built in **Phase 2** (the blog is the first loop subject — greenlight-v1.md §16). Materialized into a tool by `greenlight add`.
