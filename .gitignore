node_modules/
dist/
.turbo/
# Generated into the CLI package at build time (copy-assets.mjs)
cli/templates/
cli/assets/
.astro/
.wrangler/
*.tsbuildinfo
coverage/

# Terraform (commit .terraform.lock.hcl; ignore provider binaries + state)
.terraform/
*.tfstate
*.tfstate.*
crash.log

# Greenlight local secrets — NEVER commit (see greenlight-v1.md §14)
.greenlight/

# A real manifest is personal (the framework repo ships only the .example). Cloners commit theirs.
greenlight.config.ts

# OS / editor
.DS_Store
*.log

# Claude Code local (per-user) settings
.claude/settings.local.json

# packed tarballs (pnpm pack / inspection)
*.tgz
